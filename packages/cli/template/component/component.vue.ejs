<template>
  <div><%=name %></div>
</template>

<script lang="ts">
import { defineComponent } from 'vue'
import { createComponentName, createNamespace } from '../utils/components'
import { props } from './props'

const { b, computedClasses } = createNamespace('<%=name %>')

export default defineComponent({
  name: createComponentName('<%=bigCamelName %>'),
  props,
  setup() {
    return {
      b,
      computedClasses,
    }
  },
})
</script>
